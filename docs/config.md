# ESPHome configuration (eq3nos_bt)

Questa pagina documenta la configurazione YAML del componente **eq3nos_bt**.
Il file `.py` del custom component non è incluso nell’API reference Doxygen: lo documentiamo qui.

## Minimal example

```yaml
# TODO: incollare qui un esempio minimale reale
